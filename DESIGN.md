# taiwan-ped 遊戲設計文件

## 一句話介紹

你是一個台灣行人。在「行人地獄」的街頭活下去，能走多遠就走多遠。

3D 無盡跑酷（endless runner），同類參考：Subway Surfers、Temple Run、Crossy Road。
黑色幽默主題：障礙物是汽車、機車、違停車輛；斑馬線上車不會讓你。

## 已定案的決策（2026-09-07 與使用者確認）

| 決策 | 結果 |
|---|---|
| 主題 | 台灣行人地獄——玩家是行人，閃避台灣街頭的車 |
| 鏡頭 | **第三人稱、馬力歐賽車式低視角**（角色後方、偏低、貼近地面的壓迫感） |
| 玩家動作 | **只有左右換道**（跳躍等之後視需要再加） |
| 死亡機制 | **一撞即死，馬上重來**（每局短、重來快） |
| 平台 | 桌機瀏覽器優先，鍵盤操作；架構預留之後加手機觸控 |
| 視覺風格 | **2.5D**：3D 世界 + billboard sprite 角色車輛（馬力歐賽車 SNES/N64 風）。前期先用色塊佔位，美術里程碑再換 sprite |

## 2026-09-08 機制改版（與使用者確認）

| 決策 | 結果 |
|---|---|
| 前進方式 | **按住 ↑ 前進、↓ 後退，放開就停**。世界只在玩家走動時捲動；車輛不管你動不動都會自己衝過來 |
| 道路佈局 | 單側人行道：【人行道】【迎面車道×2】【分隔島】【對向車道×2】 |
| 背後來車 | 可玩區只有人行道＋迎面車道（所有威脅都看得到）；對向車道被分隔島擋住、純背景。之後想開放再說 |
| 靜止路障 | 違停車、擋路物。**碰到不會死，只是被擋住**。人行道為主、路邊車道偶爾有。核心循環：「安全的地方不好走，好走的地方不安全」 |
| 難度結構 | **關卡制**（tuning.ts 的 `LEVELS` 表，純後台配置玩家不可見）：每關可調 目標距離、時限、行人型態（行人速度 = 全域速度 × `playerForms[form].speed`，walker 1.0 / stroller 0.9 / wheelchair 0.7）、車流密度、車速倍率、路障密度、違停機率 |
| 命 | 三條全程共用、過關不補；失敗（被撞或超時）扣一條重來本關，用完回第一關 |
| 過關流程 | 橫幅顯示關卡資訊（型態/目標/時限）後自動開始；全過 → 恭喜畫面（佔位，細節未定）；失敗畫面版面集中在 index.html 的 #fail，之後可換素材 |

## M6 路口與佈局改版（與使用者確認）

| 決策 | 結果 |
|---|---|
| 佈局 | 分隔島拆除，改雙黃線：【左人行道】【迎面×2】【雙黃線】【對向×2】【右人行道】，全部可走 |
| 對向車 | 從玩家背後出現、會撞死人（「被突然撞」是接受的設計） |
| 路口 | 橫向小路打斷主路，永遠綠燈、橫向路無車；斑馬線全是裝飾不影響判定 |
| 右轉車 | 唯一的路口威脅。兩側路邊車道的車（卡車除外）依 turnChance 右轉：迎面車掃你左側、同向車掃你右側斑馬線 |
| 違停/路障 | 兩側路邊車道、兩側人行道都有；新增超長人行道路障（之後鋪機車停車格皮）；同向車遇違停會往內側車道繞 |
| 路口參數 | 全域在 TUNING.intersection，關卡表可選覆寫（間距、右轉機率） |
| 過關地點 | 關卡可選 `goalSide`（左/右側人行道）：走到距離還要站上該側才過關，時間照跑；終點出現目的地建築（`destination` 填 `TUNING.destinations` 總表的 key，小語和貼皮跟著來） |
| 貼皮掛載點 | `skins.ts` 集中所有世界裝飾的外觀 factory：斑馬線、路面標記、紅綠燈（永遠綠燈）、路旁建築、目的地建築。換素材只改這個檔案 |
| 行人燈 | 台灣式兩格：上格倒數數字＝本關剩餘秒數（跟 HUD 同一個計時器，最多 99）、下格小綠人走路（最後 10 秒走快）。永遠不變紅，純氣氛；小綠人圖放 `decals/signals/greenman.png`，沒圖用程式火柴人 |

## 2026-09-09 追加需求（與使用者確認）

| 決策 | 結果 |
|---|---|
| 人行道腳踏車 | 慢速、撞到也是死；每關可調生成間隔 `bikeInterval`；遇路障會往馬路那側繞（跟行人一樣被逼下馬路）。~~方向 `bikeDirs`~~ 2026-09-11 改版：方向固定同向，見下 |
| 左右車速 | 關卡表可用 `speedScaleLeft`（迎面側）/`speedScaleRight`（同向側）分開調，沒寫就用 `speedScale` |
| 斑馬線佈局 | 路口近側、遠側各一條橫向＋左右兩條縱向＝台灣常見的「口」字型 |
| 路面標記 | 「慢」與速限「50」：一組＝同一側每條車道各一字、同一個 z；字向跟該側車行方向（迎面側玩家看是顛倒的）；種類在 `roadMarkLabels` |

## 2026-09-10 無限模式（與使用者確認）→ 2026-09-13 改版，見下一節

原版：手寫關卡全過後接程序生成的「一關一關」（固定 seed、時限由距離推導、目的地隨機抽）。已被 2026-09-13 的無盡模式取代，levelgen.ts 整個重寫。

## 2026-09-13 無盡模式改版（與使用者確認）

| 決策 | 結果 |
|---|---|
| 規則 | 手寫 `LEVELS` 全過後進**一條走不完的路**：沒有終點、沒有時限；分數 = 最遠走到幾公尺 |
| 難度 | 從最簡單開始（比手寫第一關還鬆），每走 `endless.stageLength`（100 m）升一階，各參數（車流密度、車速、路障間距、違停、右轉、腳踏車）從 `*Start` 線性爬到 `*End`，`rampStages`（10 階）後停在天花板；升階時畫面浮出提示（`stageFlavors` 輪流） |
| 時限 | **沒有**：唯一死法是被車撞（含人行道腳踏車）。實作上 `timeLimit: Infinity`，行人燈倒數固定顯示 99 |
| 命 | **一條命**：死了結算「你走了 xxx m」，破紀錄會標示；按鍵從無盡起點再走一次。手寫關卡的 ❤ 在無盡模式不顯示也不扣 |
| 行人型態 | 固定步行者（速度 1.0），大家條件一樣才能比最遠距離 |
| 最遠紀錄 | 存瀏覽器 `localStorage`（`endlessBest`），開場橫幅與 HUD 第四列顯示 |
| 實作 | `levelgen.endlessLevel(maxDistance)` 依距離回傳一份 `LevelConfig`（同一階同一物件），main 每幀拿它餵 traffic / obstacles / intersections，各模組不用知道無盡模式。人行道兩側固定 normal（換鋪面要挪建築，不能中途換） |
| 測試鍵 | 3 = 跳下一關：手寫關卡一關關跳，最後一關之後是無盡模式，再按回第一關 |

## 2026-09-11 腳踏車簡化（與使用者確認）

起因：腳踏車有逆向的（左側往背後騎、右側往迎面騎），而汽車的排隊邏輯只對同方向生效，
逆向腳踏車一繞下馬路就和迎面車穿模。決定把系統改簡單：

| 決策 | 結果 |
|---|---|
| 方向 | **只有跟該側車流同向的腳踏車**：左人行道迎面騎來、右人行道從背後來。`bikeDirs` 欄位移除 |
| 繞路障 | 遇人行道路障切到路邊車道、過了切回人行道（和之前一樣） |
| 不穿模 | 切進車道後吃**和汽車同一套跟車排隊邏輯**：後車排在它後面、它排在前車後面。切出去前確認車道沒違停、沒快車會撞上來；切回來前確認人行道前方一段乾淨、沒別台腳踏車；切不出去就騎到路障前停著等。切出去（`mergeLook` 8 m）和切回來（`returnLook` 12 m）的門檻分開，才不會來回抖 |
| 路口 | 右轉車和腳踏車互相等：掃過範圍內有腳踏車，右轉車在路口停著；有右轉車接近，還沒進範圍的腳踏車停下讓車。排在對方後面（超不了車）的不算，避免互等卡死 |
| 生成規則 | 路障生成點有車（同向車/腳踏車）就晚一點再生，不再砸在車上；同一側「人行道路障 ↔ 違停」之間至少留 `passGap` 8 m，腳踏車才換得了道（不然會卡在車道上塞住後面所有車）；同一欄路障不重疊。腳踏車出生點在路口掃過範圍又有右轉車要來時不生 |
| 參數 | 全在 `TUNING.bike`；debug overlay（` 鍵）有 `stopped` / `clipping` 計數，clipping 正常永遠 0 |
| 驗證 | 無頭模擬（rolldown 把 traffic/obstacles/intersections 打包進 Node、stub 掉外觀模組）跑數十個情境 × 240 秒，0 幀重疊、無卡死 |

## 2026-09-11 路旁建築改連棟（與使用者確認）

| 決策 | 結果 |
|---|---|
| 排列 | 兩側各一排**連棟街屋**，一棟接一棟沒有空隙（面寬累加擺放；整棟捲到鏡頭後面就接到最遠那棟後面、順便重抽）。面寬窄（3.6～5.5 m）偶爾一棟寬的，樓層 2～6 層參差；參數在 `TUNING.buildings` |
| 外觀 | canvas 畫的立面貼圖做過一版，使用者覺得不像台灣、拿掉。改由**使用者自備 GLB 街屋模型**（規格見 `public/assets/models/buildings/README.md`），程式隨機抽款、依模型實際面寬接龍；沒模型時素色方塊撐著。招牌等模型到位後再談 |

## 2026-09-12 人行道鋪面樣式（與使用者確認）

| 決策 | 結果 |
|---|---|
| 三種樣式 | `normal` 綠鋪面＋「人行道」字（現狀）／`asphalt` 跟車道同色、有路緣白線、沒有字／`none` **那側只有車道**：人行道整條消失、建築貼到車道邊、行人走不進去，該側沒有停車格、路障、腳踏車、直向斑馬線。goalSide 指到 none 側時站上路邊車道就算到達 |
| 設定 | 左右各自設：關卡表 `sidewalkLeft` / `sidewalkRight`；無盡模式兩側固定 normal。執行期配置在 `tuning.ts` 的 `LAYOUT`，各模組用 `hasSidewalk` / `walkMinX` / `buildingLineX` 讀 |

## 2026-09-14 騎樓柱子碰撞撤回（使用者決定）

| 決策 | 結果 |
|---|---|
| 建築碰撞 | 改回**整棟一個框**：橫向邊界就是人行道外緣（`walkMinX` / `walkMaxX`），走不進騎樓、柱子不單獨算。9/14 凌晨做的 `TUNING.arcade`＋`world.pillarBlockers` 拿掉；`collision.ts` 抽出的 `clampScrollBy` / `blockedBy` 留著給路障用 |

## 2026-09-14 asphalt 人行道＝停車場（與使用者確認）

| 決策 | 結果 |
|---|---|
| asphalt 側的路障 | 不走一般路障池（沒有單顆 Gogoro／變電箱），改由 `obstacles.ts` 的 `updateAsphalt` **一段接一段連續鋪停車格**，段與段之間留小空隙，讓停車格佔人行道長度的比例 ≈ `TUNING.parking.asphaltCoverage`（0.8）。格子裡停不停車照 `occupancy`。無頭模擬實測扣掉路口後約 75%（路口兩側各留 1 m、零碎空位塞不下） |
| 路口 | 一段撞到路口就裁短塞進路口前的空位（後緣不動、只縮前緣；汽車格塞不下改試機車格，`asphaltFitMinStalls`），塞不下才整段挪到路口另一邊 |
| 開場 | 每關 reset 時先從出發點前方 `asphaltPrefillFrom` 公尺鋪到路障生成點，玩家一出生就看到停車格（一般路障池仍是走到 `obstacleSpawnZ` 公尺後才出現第一個；使用者決定不預鋪） |
| normal 側 | 不變：仍走路障池，`parking.chance` 只管 normal 側 |

## 2026-09-15 標題畫面、分享、鏡頭（與使用者確認）

| 決策 | 結果 |
|---|---|
| 遊戲名稱 | 「我們的賽車場」（反諷：馬路是車的賽車場）。不要副標。候選過「行人優先」「行人地獄」「平安到家」 |
| 標題畫面 | 只在開遊戲時一次，取代原本「素材載入中」的不透明橫幅；死亡重來、進下一關不經過 |
| 分享 | 只有無盡模式的結算有分享鈕（手寫關卡沒有）；文案在 `share.ts`，不進 tuning（純文案） |
| 鏡頭橫向 | 鏡頭跟人但離建築線保持距離（橫式 `cameraXMargin` 1.8、直式 `cameraPortrait.xMargin` 2.8）：直式鏡頭 4.6 m 高跟直立招牌同層，跟到牆邊會被招牌擋住 |
| 後方來車提示 | 朋友反映看不到背後的車 → 同向車車頭燈光暈打在前方地面（`TUNING.headlight`），車還在畫面外光先進畫面；後照鏡（多畫一次場景）與車道雷達（HUD）列為備案 |

## 2026-09-12 視角切換 → 2026-09-14 拿掉第一人稱（與使用者確認）

曾做過兩個視角可切換（視角 1 第一人稱：鏡頭在眼睛位置、跟著人物朝向轉、按鍵是畫面座標；視角 2 第三人稱）。
2026-09-14 決定**只留第三人稱**，第一人稱整個移除（程式碼、按鈕、C 鍵、`firstPerson` tuning 都拿掉）。

| 決策 | 理由 |
|---|---|
| 放棄第一人稱 | 最致命的車從背後來，第一人稱看不到，只能盲猜「!」；為了不暈疊了方向鎖定、視線分段、慢轉身等一堆補丁，互相牽制；手機搖桿下方向與視線的關係更難懂 |
| 保留 | 後方來車「!」警示與喇叭（第三人稱下背後的車也是從畫面底部才冒出來） |
| 試過放棄的 | 橫移時微微轉頭＋側傾、視線焦點偏移（路面會斜、暈）、鏡頭平移、第一人稱 8/16 段視線 snap。實驗分支 `exp/camera-turn` |

## 核心架構訣竅（不可違反）

1. **玩家不會動，世界往鏡頭捲過來。** 主角永遠固定在 Z=0 附近；
   「前進」= 世界往 +Z 捲動，捲動速度由玩家按住 ↑/↓ 決定（放開 = 0）。
   車輛在捲動之外還有自己的車速，所以玩家站著不動車照樣衝過來。
   好處：不需要無限地圖、沒有座標精度問題、物件回收簡單。
2. **碰撞不用物理引擎。** AABB（軸對齊方框）重疊判定，十幾行程式。
   唯一依賴是 Three.js。
3. **難度是幾根可調的桿子。** 車輛生成密度、車種組合（機車快/卡車大）、
   玩家型態（單人 → 嬰兒車 → 輪椅，碰撞體積遞增）——全部資料驅動，
   定義在 tuning.ts 的 `vehicles` 與 `playerForms`，難度系統（M5）直接引用。
4. **所有可調參數集中在 `src/tuning.ts`。** 鏡頭高度/距離/FOV、車道寬、
   速度、生成間隔……改數字存檔就能立刻感受差異，手感靠玩出來不靠猜。

## 節奏設計

- 前進閃避段（縱向、持續壓力）→ 斑馬線段（橫向、瞬間決策，Frogger 式）→ 回到前進段。
- 斑馬線段是主題核心哏（車不讓行人）：M6 路口做完，右轉車掃過斑馬線是唯一的路口威脅。
- 碰撞判定從寬（碰撞箱比視覺模型小一圈）：玩家覺得「差點被撞到」比「明明沒碰到卻死了」好一百倍。

## 里程碑

- [x] M0 需求釐清、設計文件
- [x] M1 一條路 + 世界捲動 + 左右換道（第三人稱鏡頭、tuning.ts）
- [x] M2 車輛生成 + AABB 碰撞 + 死亡重來 ← **到這裡就是完整可玩的遊戲**
- [x] M3 操作改版：↑前進↓後退、雙向道路佈局（迎面側可玩、對向背景、分隔島）
- [x] M4 人行道 + 靜止路障（擋路不致死、「被逼下馬路」循環）、停車格路段、人行道腳踏車
- [x] M5 關卡制難度系統（LEVELS 表、五條命、過關橫幅、抵達/失敗畫面）+ debug overlay（` 鍵，含各模組耗時、繪製次數）
- [x] M6 路口＋佈局改版（雙黃線全馬路可走、右轉車、超長路障、intersections.ts）
- [x] M6.5 目的地系統（`TUNING.destinations` 總表：招牌字→小語→立面圖貼皮；HUD 掛任務；抵達畫面）
- [x] M7 無盡模式（2026-09-13 改版：一條走不完的路、每 100 m 升階、沒時限、一條命、最遠距離存 localStorage、每階換背景淡入淡出）
- [ ] M7.5 程序生成路段變化（每段路不同人行道鋪面、車道數變化；目前佈局是整關固定、車道數是全域常數）
- [x] M8 手機支援（虛擬搖桿、直式鏡頭自適應、GitHub Pages 自動部署）
      正式網址：https://jingjeryen.github.io/game-pedestrian-hell/
- [x] M8.5 素材換皮：Kenney 角色/車輛、街屋產生器 GLB、程式組的嬰兒車、同向車車頭燈光暈（後方來車提示）、撞擊效果
- [x] M9 音效（2026-09-15：Web Audio 一套 10 個音，全部程式合成 → 零檔案零下載；
      音檔可覆蓋、每個音效可單獨關、玩家按 M 全開/全關。見 `src/sfx.ts`、`public/assets/sfx/README.md`）
- [x] M9.1 開場標題畫面（2026-09-15：遊戲名「我們的賽車場」、素材載入進度條、操作說明、最遠紀錄；
      載入中不透明底、到齊淡出讓場景浮現；點一下開始 = 順便取得手機播音授權。文案在 `TUNING.title`）
- [ ] M9 其餘：UI 正式版（抵達/失敗畫面）、風格統一（motor1 換積木風）
- [ ] M9.5 效能：街屋五種材質合一（繪製次數 −1/3）、解析度上限
- [x] M10 分享連結（2026-09-15：無盡模式結算「分享成績」鈕，手機系統分享面板／電腦複製文字＋網址；
      網址帶 `?challenge=公尺數`，朋友點開標題畫面顯示挑戰。見 `src/share.ts`）
- [ ] M10.5 排行榜（要後端；非同步比分是雙人玩法的入門版，即時同場連線另議）

### 試過又拿掉的

- 第一人稱視角（2026-09-14）：看不到背後來車、容易暈，決策見上面「拿掉第一人稱」一節
- 騎樓可走進去＋柱子碰撞（2026-09-14）：柱子擋人的手感不好，改回整棟一個框

## 技術棧

WSL2 + Three.js + TypeScript + Vite。runtime 依賴只有 `three`。
